#ifndef MAIN_H
#define MAIN_H
#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>

void	_putchar(char c, int *count);
void	_putstr(char *str, int *count);
int	_printf(const char *formart, ...);

#endif
